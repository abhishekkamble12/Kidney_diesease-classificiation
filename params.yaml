# Parameters for training and model selection
model_name: "LogisticRegression"  # Options: "MultinomialNB", "LogisticRegression", "GlobalPoolingFFN", "DistilBERT"

# Training hyperparameters for FFN / DistilBERT
epochs: 3
batch_size: 64
learning_rate: 0.0001
max_features: 25000
embedding_dim: 128
max_len: 128
